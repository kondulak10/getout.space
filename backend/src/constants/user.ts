/**
 * User-related constants
 */

/**
 * Number of activities a user must complete before being prompted to add their email
 * This prevents spam and ensures users are engaged before we ask for contact info
 */
export const EMAIL_COLLECTION_ACTIVITY_THRESHOLD = 3;
